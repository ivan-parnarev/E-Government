import Login from '../../components/BusinessAuth/Login/Login';
import styles from './LoginPage.module.css';

export default function LoginPage() {
    return (
        <main className={styles.container}>
            <Login />
        </main>
    );
}
